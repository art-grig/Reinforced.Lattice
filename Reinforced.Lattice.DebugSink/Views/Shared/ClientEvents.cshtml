<script type="text/javascript">
    var objectEventHandler = function(obj) {
        alert(obj.Name + ' ' + obj.Preorders);
    }

    var selectorEventHandler = function (obj) {
        alert('Mesage from selector: ' + obj.Name + ' ' + obj.Preorders);
    }

    var rowEventHandler = function (obj) {
        alert('Row tells us: ' + obj.Name + ' ' + obj.Preorders);
    }

    var rowSelectorEventHandler = function (obj) {
        alert('Row-Selector tells us: ' + obj.Name + ' ' + obj.Preorders);
    }
</script>
